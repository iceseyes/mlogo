language: cpp

compiler:  
  - gcc
  - clang

addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - cmake

before_script:  
  - mkdir build
  - cd build
  - cmake ..

script:
  - make
  - test/mlogo_test
